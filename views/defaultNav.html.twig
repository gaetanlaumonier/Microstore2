{% block pageTopBar %}
	<nav class="navbar navbar-inverse">
		<div class="container-fluid">
			<div class="navbar-header">
				<button type="button" class="navbar-toggle" data-toggle="collapse"
				data-target=".navbar-ex1-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				</button>
				<a class="navbar-brand" rel="home" href="{{ path('home') }}" title="Le Fruit, le Béret, la Cocarde">
					Le Fruit, le Béret, la Cocarde
				</a>
				<a class="navbar-brand" href="#">
                   <img class="navBarLogo" src="/pics/LogoC.png" alt="">
                </a>
				<a class="navbar-brand"  href="{{ path('produit') }}"> Nos produits

				</a>
			</div>
			<div>
				<ul class="nav navbar-nav navbar-right">
                        {% if app.user and is_granted('IS_AUTHENTICATED_FULLY') %}
                            <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <span class="glyphicon glyphicon-user"></span> Bienvenue, {{ app.user.username }} <b class="caret"></b></a>
                                <ul class="dropdown-menu">
                        			{% if is_granted('ROLE_ADMIN') %}	
                        			<li><a href="{{ path('backoffice') }}"><span class="glyphicon glyphicon-cog"></span> Menu administrateur</a></li>
                        			{% endif %}
                        			<li><a href="{{ path('panier') }}"><span class="glyphicon glyphicon-shopping-cart"></span> Panier</a></li>
                        			<li><a href="{{ path('profil') }}"><span class="glyphicon glyphicon-user"></span> Profil</a></li>
                                    <li><a href="{{ path('logout') }}"><span class="glyphicon glyphicon-log-out"></span> Déconnexion</a></li>
                                </ul>
                            </li>
                        {% else %}
                            <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                <span class="glyphicon glyphicon-user"></span> Vous n'êtes pas connecté <b class="caret"></b></a>
                                <ul class="dropdown-menu">
                                    <li><a href="{{ path('login') }}">Connexion</a></li>
                                    <li><a href="{{ path('register') }}">Inscription</a></li>
                                </ul>
                            </li>
                        {% endif %}
				</ul>
			</div>
		</div>
	</nav>
{% endblock %}